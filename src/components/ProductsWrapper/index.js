import React from 'react';
import StyledProductsWrapper from './styles'

const ProductsWrapper = ({ children }) => {
  return <StyledProductsWrapper>{children}</StyledProductsWrapper>
}


export default ProductsWrapper;